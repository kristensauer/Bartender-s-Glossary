---
import Layout from "@layouts/Layout.astro";
import SampleComponent from "@components/SampleComponent.astro";
import Container from "@components/Container.astro";
import { Content as SampleMarkdown } from "@content/sample-markdown.md";
import { Image } from "astro:assets";
import TwoColumns from "@components/TwoColumns.astro";
import ToolSection from "@components/ToolSection.astro";
import CocktailsSection from "@components/CocktailsSection.astro";
import ShotsSection from "@components/ShotsSection.astro";
---

<Layout title="Astro Starter">
  <header>
    <div class="hero-image-container">
      <img src="/images/header.jpeg" alt="Group of drinks" />
      <div class="hero-overlay">
        <h1 class="hero-title">Bartender's Glossary</h1>
        <p class="hero-subtitle">
          Shake, Stir, and Serve</p>
          <p class="hero-subtitle2">like a pro</p>
        <div class="nav-buttons-container">
          <button class="nav-btn" onclick="location.href='#cocktails'"
            >Cocktails</button>
          <button class="nav-btn" onclick="location.href='#shots'">Shots</button>
          <button class="nav-btn" onclick="location.href='#tools'">Tools</button>
      </div>
      </div>
    </div>
  </header>
  <body>
    <div
      style="z-index:0; position:relative; margin-bottom:0; padding-bottom:0;"
    >
        <div style="display: flex; justify-content: flex-end; z-index:0; position:relative; margin-bottom:-3rem; padding-bottom:0;">
          <CocktailsSection />
        </div>
    </div>
    <div class="hand-png-container">
      <img
        class="hand-png"
        src="/images/holding.png"
        alt="Someone holding a cocktail"
      />
    </div>
    <div class="shots-background-content-box">
      <div id="shots-section">
        <ShotsSection />
      </div>
    </div>
    <div style="position: relative;">
      <ToolSection />
      <img src="/images/3.png" alt="Decorative" class="tools-bleed-img" />
      <img src="/images/2.png" alt="Decorative" class="tools-bleed-img-right" />
    </div>
    <footer class="site-footer">
      <div
        class="footer-content-box"
        role="contentinfo"
        aria-label="Site footnote and navigation"
      >
        <nav class="footer-nav" aria-label="Footer navigation">
          <a href="#cocktails" class="footer-link" tabindex="0">Cocktails</a>
          <span aria-hidden="true">|</span>
          <a href="#shots" class="footer-link" tabindex="0">Shots</a>
          <span aria-hidden="true">|</span>
          <a href="#tools" class="footer-link" tabindex="0">Tools</a>
        </nav>
        <div class="footer-title">Bartender's Glossary</div>
        <div class="footer-meta">
          &copy; 2025 Bartender's Glossary. All rights reserved.
        </div>
      </div>
    </footer>
  </body>
</Layout>

<style>
  .hero-image-container {
    position: relative;
    width: 100vw;
    max-width: 100%;
    margin: 0 auto;
    padding: 0;
    overflow: hidden;
  }
  .hero-image-container img {
    width: 100vw;
    max-width: 100%;
    height: auto;
    display: block;
    object-fit: cover;
    margin: 0;
    padding: 0;
  }
  .hero-overlay {
    position: absolute;
    top: 5%;
    left: 0;
    width: 100%;
    height: 100%;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: flex-start;
    z-index: 2;
    pointer-events: none;
  }
  .hero-title {
    color: var(--color-background, #fff);
    font-size: 1.0vw;
    font-family: var(--font-heading);
    font-weight: bold;
    margin-top: 20vw;
    margin-bottom: 1.2rem;
    text-shadow: 0 2px 12px rgba(0, 0, 0, 0.3);
    padding: 0.25rem 1.5rem;
    white-space: nowrap;
    pointer-events: auto;
    z-index: 3;
    text-transform: uppercase;
  }
  .hero-subtitle {
    color: var(--color-accent);
    font-size: 1.2rem;
    font-family: var(--font-body);
    margin-top: 20vw;
    margin-bottom: 1rem;
    text-shadow: 0 1px 8px rgba(0, 0, 0, 0.3);
    padding: 0.25rem 1rem;
    pointer-events: auto;
    z-index: 3;
    text-transform: uppercase;
  }
  .nav-buttons-container {
    display: flex;
    justify-content: center;
    gap: 2rem;
    margin-top: 15vw;
    margin-bottom: 0rem;
    pointer-events: auto;
    z-index: 3;
  }

  .nav-btn {
  min-width: 160px;
  min-height: 56px;
  padding: 1rem 1.5rem;
  margin: 0;
  margin-top: 25px;
  border-radius: 2.5rem;
  font-size: 1.5rem;
  background: var(--color-text);
  color: var(--color-background);
  border: none;
  font-family: var(--font-heading);
  font-weight: bold;
  cursor: pointer;
  box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
  display: flex;
  align-items: center;
  justify-content: center;
        transition:
    background 0.4s,
    color 0.4s,
    transform 0.4s;
}
  .nav-btn:hover,
  .nav-btn:focus {
    background: var(--color-primary);
    color: var(--color-text);
    outline: none;
      transition:
    background 0.4s,
    color 0.4s,
    transform 0.4s;
  }

  .hand-png {
    position: absolute;
    z-index: -10;
    width: 50vw;
    max-width: 600px;
    min-width: 120px;
    height: auto;
    object-fit: contain;
    display: block;
    margin-top: 0;
    margin-bottom: 0;
    margin-right: 0;
    margin-left: auto;
    box-shadow: none;
    filter: none;
    background: transparent;
    padding: 0;
    pointer-events: none;
  }

  img {
    width: 100vw;
    height: 500px;
    object-fit: cover;
    display: block;
    z-index: 1;
  }

  header {
    position: relative;
    text-align: center;
    z-index: 3;
  }

  .hero-image-container {
    position: relative;
    width: 100vw;
    max-width: 100%;
    height: 70%;
    overflow: hidden;
    box-shadow: 0 4px 24px rgba(0, 0, 0, 0.1);
    background: linear-gradient(
      90deg,
      var(--color-secondary) 0%,
      var(--color-background) 100%
    );
  }
  .hero-image-container img {
    width: 100vw;
    height: 100%;
    object-fit: cover;
    display: block;
    filter: brightness(0.95) contrast(1.05) saturate(1.1);
    box-shadow: 0 2px 16px rgba(0, 0, 0, 0.12);
    transition: filter 0.3s;
  }
  .hero-image-container h1 {
    position: absolute;
    top: 10%;
    left: 50%;
    transform: translate(-50%, -50%);
    color: var(--color-text);
    text-shadow: 0 2px 8px rgba(0, 0, 0, 0.5);
    margin: 0;
    margin-top: -4vw;
    font-size: 1.5vw;
    letter-spacing: 0.5px;
    font-family: var(--font-body);
    background: rgba(0, 0, 0, 0.15);
    padding: 0.5rem 2rem;
    border-radius: 1rem;
  }
  .hero-image-container p.hero-subtitle {
    position: absolute;
    top: 12%;
    left: 50%;
    transform: translate(-50%, -50%);
    color: var(--color-accent);
    text-shadow: 0 1px 8px rgba(0, 0, 0, 0.3);
    margin: 0px;
    margin-bottom: 1vw;
    font-size: 3.5vw;
    font-family: var(--font-heading);
    font-weight: bold;
    padding: 0.25rem 1rem;
    pointer-events: none;
    z-index: 3;
    white-space: nowrap;
  }

    .hero-image-container p.hero-subtitle2 {
    position: absolute;
    top: 17%;
    left: 50%;
    transform: translate(-50%, -50%);
    color: #e2e2e1;
    text-shadow: 0 1px 8px rgba(0, 0, 0, 0.3);
    margin: 0px;
    margin-bottom: 1vw;
    font-size: 3.5vw;
    font-family: var(--font-special);
    font-weight: bold;
    padding: 0.25rem 1rem;
    letter-spacing: 1px;
    pointer-events: none;
    z-index: 3;
    white-space: nowrap;
  }

  .image-container img:hover {
    filter: brightness(1) contrast(1.1) saturate(1.2);
  }

  .hand-png-container {
    width: auto;
    display: block;
    justify-content: flex-start;
    align-items: center;
    margin: 0;
    margin-bottom: 0;
    padding-bottom: 0;
    background: transparent;
    transform: scaleX(-1);
    z-index: -1000;
    position: relative;
  }

  .hand-png {
    background: transparent;
    width: 50vw;
    max-width: 600px;
    min-width: 120px;
    height: auto;
    object-fit: contain;
    display: block;
      margin-top: -58rem;
    margin-bottom: 0;
    margin-right: 0;
    margin-left: auto;
    box-shadow: none;
    filter: none;
    z-index: -10;
    position: relative;
    padding: 0;
  }

    .shots-background-content-box {
    position: relative;
    background: var(--color-secondary);
    padding: 1.5vw;
    margin: 2vw 0;
    z-index: 0;
    min-height: 300px;
    width: 100%;
    box-shadow: 0 2px 8px rgba(0,0,0,0.04);
  }

    .tools-bleed-img {
    position: absolute;
    left: 0;
    bottom: -2vw;
    width: 30vw;
    max-width: 600px;
    min-width: 120px;
    z-index: 9999;
    pointer-events: none;
    filter: drop-shadow(0 2px 8px rgba(0,0,0,0.10));
  }
  .tools-bleed-img-right {
    position: absolute;
    right: 0;
    bottom: 12vw;
    width: 40vw;
    max-width: 600px;
    min-width: 120px;
    z-index: 9999;
    pointer-events: none;
    filter: drop-shadow(0 2px 8px rgba(0,0,0,0.10));
  }
  
  .site-footer {
    width: 100%;
    background: transparent;
    color: var(--color-text);
    text-align: center;
    font-family: var(--font-body);
    margin-top: 3rem;
  }
  .footer-nav {
    font-size: 1.1rem;
    margin-bottom: 0.5rem;
    display: flex;
    gap: 0.5rem;
    flex-wrap: wrap;
    align-items: center;
    justify-content: center;
  }
  .footer-link {
    color: var(--color-text);
    text-decoration: none;
    font-weight: bold;
    font-family: var(--font-heading);
    margin: 0 0.5rem;
    padding: 0.25rem 0.5rem;
    border-radius: 2.5rem;
    outline: none;
    transition: 0.4s,
      background 0.4s,
      color 0.4s;
  }
  .footer-link:focus,
  .footer-link:hover {
    background: var(--color-text);
    color: var(--color-background);
    text-decoration: none;
    transition: 0.4s,
      background 0.4s,
      color 0.4s;
  }
  .footer-title {
    font-family: var(--font-heading);
    color: var(--color-text);
    font-size: 1.3rem;
    font-weight: bold;
    margin-bottom: 0.5rem;
    letter-spacing: 1px;
  }
  .footer-meta {
    font-size: 0.9rem;
    color: var(--color-text);
    opacity: 0.8;
  }
    .footer-content-box {
    background: var(--color-secondary);
    box-shadow: 0 2px 8px rgba(0,0,0,0.04);
    padding: 2rem 1rem;
    margin: 2rem auto 0 auto;
    width: 100%;
    color: var(--color-text);
  }
</style>
